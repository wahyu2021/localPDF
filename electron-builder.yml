appId: com.localpdf.app
productName: LocalPDF
copyright: Copyright © 2026

directories:
  output: build

# Arahkan ke hasil compile TypeScript di dist/
files:
  - dist/**/*
  - public/**/*
  - "!src"
  - "!binaries"
  - "!temp"
  - "!build"
  - "!node_modules"

# Binary engine di-copy ke luar asar supaya bisa dieksekusi langsung
extraResources:
  - from: binaries/${os}/
    to: binaries/
    filter:
      - "**/*"

win:
  target: nsis
  icon: public/icon.ico

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: LocalPDF

# Placeholder untuk build macOS/Linux di v2
# mac:
#   target: dmg
#   icon: public/icon.icns
# linux:
#   target: AppImage
#   icon: public/icon.png
